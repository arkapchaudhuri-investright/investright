# --- Secrets: NEVER commit (Gemini key lives here) ---
.env
.env.*
*.env

# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- App data (SQLite DB, logs, caches — regenerated by cron/refresh) ---
data/
*.db
*.sqlite
*.sqlite3
*.log

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*.Rhistory
.idea/
.vscode/
.claude/

# --- Local scratch / deploy prompts (not part of the app) ---
PHASE5_PROMPT.md

# NOTE: static/arka.jpg IS intentionally tracked (public team photo).
# The source upload static/0cbc74a6-*.JPG is ignored below.
static/0cbc74a6-*.JPG

# Company logos are fetched + cached at ingest (Phase 9), not source — they
# repopulate on the VM via the nightly refresh / any /analyze. Don't commit them.
static/logos/
static/execs/
